/** Logo-only header height when chrome is collapsed. */
export const HOME_COLLAPSED_HEADER_HEIGHT = 56;

/** Spring config for chrome show/hide transitions. */
export const HOME_CHROME_SPRING = { damping: 24, stiffness: 260, mass: 0.7 };

/** Minimum scroll delta (px) before toggling chrome visibility. */
export const HOME_SCROLL_DIRECTION_THRESHOLD = 1.5;

/** Scroll offset at or below which chrome is always shown. */
export const HOME_CHROME_SHOW_AT_TOP_OFFSET = 8;

/** Collapsed FAB diameter. */
export const HOME_FAB_COLLAPSED_SIZE = 52;

/** Expanded FAB width (icon + label). */
export const HOME_FAB_EXPANDED_WIDTH = 190;
