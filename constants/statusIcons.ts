import { colors } from './colors';

/** Status icon size on question cards and list items (e.g. Home feed rows). */
export const STATUS_ICON_QUESTION_ITEM_SIZE = 24;

/** Gray-black glyph color shared by every status icon (cards + filter tablets). */
export const STATUS_ICON_NEUTRAL_COLOR = colors.MEDIUM_GRAY;
